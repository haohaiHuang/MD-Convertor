# Changelog

[English](CHANGELOG.md) | **简体中文**

本文件记录项目中面向用户或协作者的重要变化，格式参考 Keep a Changelog；尚未归入版本的变化记录在 `[Unreleased]`。

## [Unreleased]

### 新增

- 增加 Chromium 浏览器插件（代码在 `extension/`）：点一下工具栏图标，当前页面就会存成 `<标题>.md` 加一个 `<标题>.images/` 目录。它不需要桌面端也不需要后台服务，需要登录会话才可访问的图片同样下得来；Markdown 里的每处图片引用都会被改写成真正落在它旁边的那个文件名，下载失败的图片保留原 URL 并在下一行附一行 `<!-- 图片未下载：… -->`，原因在文档里看得见。同一页再导一次会覆盖上一对文件，不留 `(1)` 副本。插件尚未上架应用商店，目前从 `extension/dist` 以「加载已解压的扩展程序」方式使用。

## [0.3.6] - 2026-09-22

### 新增

- 增加 Markdown 默认保存目录，不必每次下载都选一遍位置。设置页新增「输出」卡片：选一次目录（用 macOS 原生的目录选择框，可以在里面直接新建文件夹），再用「使用默认目录」开关决定是否启用。开关开着时，点「下载」会把文件直接写进该目录，并显示实际落盘的完整路径；开关关着时行为与之前完全一致。

- 直接保存被拒绝时不再静默失败。目录不存在、磁盘只读、空间不足或没有写入权限，都会在结果区说明具体原因，随后自动改走浏览器下载 —— 两条路都不会让用户拿不到文件。该提示会在下一次转换开始时清除；浏览器里没有桌面桥接，行为保持原样不变。

### 变更

- 设置文件新增 `output` 字段。旧版本写出的设置文件仍按原样读入：缺这个字段时按默认值补齐，不会被判为文件损坏，因此升级不会丢掉任何已配置的 Provider、语言或翻译设置；存储格式版本号不变。

- 应用包现在只装它真正会读的东西。此前打包产物把整个仓库都装了进去 —— 项目文档、源码、测试、构建配置，以及内部工作笔记 —— 应用一个都不读，却会随下载一起发布出去。现在包里只留应用清单和启动时加载的 Electron 侧模块，包内条目从 253 条降到 10 条。

### 修复

- iCloud 云盘里的文件夹现在可以作为默认保存目录。桌面桥接的路径校验把路径中出现的每个 `~` 都当成家目录简写，而 iCloud 云盘的数据正落在 `com~apple~CloudDocs` 下 —— 于是「选择目录」在这里选出来的目录会被判定为非法；更糟的是这次拒绝是以 promise 拒绝（reject）抛出的，调用处没有捕获，结果点「下载」看起来完全没反应：不写文件、不走浏览器下载、也不报错。现在 `~` 只在「位于路径段开头」时才算家目录简写；其余任何拒绝都会在结果区说明原因，并降级为浏览器下载，不再无声消失。

- 默认保存目录直写成功时，现在有明显确认。因为这套功能的意义恰恰在于「不弹保存框」，所以原先没有任何东西能证明下载成功：文件落盘了，页面却看不出变化 —— 唯一那行确认还是灰色小字，跟正文说明长一个样。现在「下载」按钮会当场变成「已保存」并短暂停留（与旁边的「复制」变成「已复制」同一套做法），结果区的确认也从纯文字改成带 ✓ 的状态卡片。写入被拒也不再和成功长得一样：失败用警告色，成功用主色底。

## [0.3.5] - 2026-09-21

### 变更

- 界面配色由墨绿改为海军蓝：三个 accent token（`#176b5d` / `#0f5147` / `#dcece7` → `#2A395C` / `#232F4E` / `#EEF1F6`）、四个带绿底的中性 token（`--ink`、`--muted`、`--line`、`--paper`）、所有染色型字面量（阴影、聚焦环、对话框遮罩、半透明底）、深色代码块（`#202a28` → `#1E222B`）与浏览器标签页 favicon 全部换到同一色族；每个半透明值的 alpha 保持不变，警告色与错误色也不动。设置页的 Provider 卡片同时改掉一个从未定义、只靠兜底值工作的 `--surface-muted`，改用页面背景。对比度持平或更好：正文在页面上 15.2:1，次要灰 6.3:1（此前 4.7:1；真机复核认为备注文字看不清，已从计划的 4.6:1 往深调一档），白字压主色 11.4:1，代码文字压深色底 13.8:1。应用文字也有变化：十个手调字重收敛为同一档 Regular，以 `--weight-body` 与 `--weight-ui` 两个变量暴露（当前同为 `400` —— 正文曾短暂压到 `300`，真机复核认定小字识别度不足，已回到 Regular）；`.preview` 守卫把 Markdown 阅读区钉在界面字重上，以后调 `--weight-body` 也漏不进阅读区（阅读区的粗体标题同样保持不变）；并统一打开 `-webkit-font-smoothing: antialiased`，让应用文字与阅读区用同一种渲染平滑。字号、字族、间距与配色均未改动。

- 富文本面板的「清空」按钮不再单独占一行：它和来源 URL 输入框、「转换」按钮同处一行，紧挨在「转换」左侧 —— 与链接面板里「清空链接」的位置一致；「转换」右边缘与正文框对齐的关系保持不变。

## [0.3.4] - 2026-09-21

### 新增

- 链接抓取失败时给出出路：当链接转换在服务端失败（需要登录、被反爬拦截、拒绝访问或超时），错误提示下方会出现一行提示，把同一个页面改用粘贴转换 —— 点击即切到「富文本转换」并把焦点移过去，在浏览器里已经能看到的正文照样可以转成 Markdown。只有抓取失败才显示这行提示：抓取成功的页面（或链接本身格式不对）不会出现该建议。

### 变更

- 打包应用现在带自己的图标，而不再是 Electron 默认图标：打包器改为指向 `assets/icon.icns`（与它的 1024px 母版一起放在仓库里），因此应用包、程序坞图标与访达条目都显示 MD 标识；在此之前包内一直是原样的 `electron.icns`。
- 云端卡片现在只有一个重置动作，且清的不是密钥一项： 「清除」移到卡片头部与「保存」并列，一下同时删掉已保存的密钥、清空四个字段、删掉已保存的这条 Provider —— 卡片回到「未配置」。因此更换密钥就是要清掉后重填整张卡片；密钥行里不再有单独的按钮。

## [0.3.3] - 2026-09-20

### 变更

- 应用不再附带第二份未被使用的 Electron 运行时。Next.js 的输出追踪跟随 Playwright 的可选 Electron 启动器，把整个 `electron` 包（解压后 295 MB，占应用 35%）拷进了内置服务目录，而应用自身代码从不加载它。桌面准备脚本现在会在追踪结束后删除 `server/node_modules/electron`，分发 ZIP 因此从 `358,726,788` 降到 `232,947,408` bytes（小 120 MiB），未压缩应用从 843 MB 降到 539 MB。Playwright、Playwright Core、Sharp 的 arm64 包与内置 Chromium Headless Shell 全部保留：应用用自己的无头浏览器（`PLAYWRIGHT_EXECUTABLE_PATH`），不走 Electron。

## [0.3.2] - 2026-09-20

### 修复

- 修复运行应用时程序坞出现第二个持续跳动的「黑底 exec 图标」的问题。本地服务进程原先用应用自己的可执行文件启动，macOS 因此把它当成第二次启动 MD-Convertor，而它永远不会完成注册；现在改用包内的 `MD-Convertor Helper`（该 bundle 声明了 `LSUIElement`）启动，运行时不变，但不再出现在程序坞。

## [0.3.1] - 2026-09-20

### 变更

- 云端配置改为「单张卡片 + 单一 Provider」：一个「保存」按钮写入名称、Base URL、API 密钥、模型四项，缺哪一项就在卡片上用警告色提示哪一项且不写盘。删除 Provider 列表、「添加 Provider」按钮、常驻的「新建 Provider」卡片、「设为当前 Provider」单选、「当前使用」徽标与「删除」按钮（「环境变量名」字段与独立的「保存 Provider」「保存密钥」按钮早先已删）。旧文件里仍有多条 Provider 时，只编辑当前生效的那一条，保存后收敛为一条。
- 「拉取模型」直接用刚填的地址与密钥读取端点模型列表，且只读取、不写设置；模型字段是可自由填写的输入框（拉取结果作为候选），端点不可达时仍能手填保存。
- 已保存的密钥不再回填输入框：输入框以八个黑点作为占位并提示「已保存，先清除密钥再更换」，已保存密钥时为只读（占位符不可选中、不可复制、不会随表单提交，页面依旧不会从密钥库回填明文），只有点「清除密钥」才能更换。
- 设置页界面调整：当前生效方式改为带标题的卡片，不再显示「当前生效」标签（切换时它会挤动两个选项的位置）；隐藏「自定义语言标签」入口（此前已保存的自定义标签仍会出现在目标语言列表中）；「返回转换」改为按钮并带保存状态胶囊，点击会等待在途保存，保存失败则不会离开页面。
- 主页页头去掉「本机处理 · 不保存内容」提示与 ⚙ 图标，只保留带文字的「设置」按钮。
- 两个面板的转换按钮都改名为「转换」（原「转换为 MD」）；富文本面板的转换按钮与「来源 URL」输入框同行，且按钮右边缘与上方粘贴框右边缘对齐。
- 首页与设置页页头去掉绿色「MD」方块，只保留纯文字品牌字，字体改为 Michroma。字体文件存放在仓库内（`public/fonts/Michroma-Regular.woff2`，许可证文件与其同目录），通过 `next/font/local` 加载，因此构建过程完全不需要联网；打包后的应用由自己的 `/_next/static/media` 提供构建时用的同一份文件，不会请求 Google。

### 修复

- 不再提供删除 Provider 的动作，密钥通过「清除密钥」移除。Provider 密钥不再支持环境变量来源，保存密钥成为唯一的配置方式。
- 修复长文翻译在固定 120 秒任务上限处被中止的问题：任务总预算改为按实际批次数动态计算（每批 60s + 30s 固定余量，下限 120s）；200,000 字符上限与取消行为不变。
- 修复云端翻译报「Provider … 返回了无法识别的回答」的问题。实际有两个错：单次调用 60s 上限对云端推理模型太短（实测一次小批量翻译要 38–60s 的推理 token），而在读响应体途中发生的超时被误报成「无法识别的回答」而不是超时。现在单次上限为 180s，任务预算随之调整（每批 180s + 30s 固定余量）；这类中止会像其它中止一样报超时（或取消）。

### 安全

- 把 Next.js 升到 16.3.5、sharp 升到 0.35.4，清除两者已公布的公告（影响 Windows 托管 Next.js 服务的未认证 RCE、sharp 的 libheif 问题）。`npm audit --omit=dev` 现在不再报生产依赖漏洞。

## [0.3.0] - 2026-09-18

### 新增

- 新增设置页，可从页头「⚙ 设置」按钮进入；设置保存在本机 `settings.json`，通过 `GET/PUT /api/settings` 读写，包含当前生效方式（云端 Provider / 本地 CLI）、目标语言和「转换时默认翻译正文」开关。
- 新增云端 Provider 管理：可添加、改名、改地址、选为当前 Provider 和删除；可拉取 OpenAI 兼容端点的模型列表、选择模型或手填模型名。删除 Provider 会二次确认。
- 新增本地 CLI 探测：设置页按当前 `PATH` 扫描，显示 `pi` 与 `claude` 的所在路径，可拉取 CLI 自报的模型列表、启用或停用某个 CLI，并按 CLI 选择模型。
- 新增目标语言选择：11 个预置语言，可附加自定义 BCP-47 标签；源语言固定显示为「自动识别（由模型判定）」。
- 新增正文翻译：链接面板与富文本粘贴面板共用一个「翻译为 <目标语言>」勾选框，初始值取设置里的默认开关；勾选后转换成功即自动翻译。结果区新增「原文 / 译文」双 Tab（可用方向键切换），复制与下载作用于当前 Tab，译文下载名为 `<原名>-<目标语言>.md`（如 `文章-en.md`）。
- 翻译过程显示进度并可「取消」；失败时译文 Tab 显示原因与「重试」按钮（复用已完成的语言判定，不重复判定）。未配置可用模型时不显示 Tab，只提示前往设置页。
- 新增翻译前的正文语言判定：正文大部分已是目标语言时先询问再翻译。占比 ≥97% 只提示一行并跳过翻译；70%–97% 弹出确认框，可选「只翻译非目标语言部分」或「不翻译」（后者保留原文、保持勾选状态、不再重复弹窗）；低于 70% 不打扰，直接全文翻译。保留原文的段落逐字节不变。
- 新增仅桌面应用可见的密钥面板，通过 Electron `safeStorage` 把 Provider 密钥保存在 macOS 钥匙串；密钥不会写入 `settings.json`、不会进入本地服务或日志，在非打包环境中不显示该面板。保存或删除密钥后无需重启即刻生效，删除后会回退到该 Provider 配置的环境变量或 `.env`。

### 变更

- 设置页的云端 Provider、本地代理和语言三节不再是占位内容。翻译功能现已可用：只有勾选翻译并完成一次转换时，正文才会发送到你自行配置的本机 CLI 或云端 Provider；MD-Convertor 自身仍不上传内容、不保存正文、不建立历史记录。

### 说明

- `0.3.0` 产物未做 Developer ID 签名与 notarization，仅适合个人测试。
- 发布门禁不再因为本机缺少历史发布 ZIP 而阻断。0.1.0–0.2.0 归档与 0.1.3 只读副本已丢失且无法恢复；仍然存在的归档照旧逐字节校验哈希，退役项会在每次发布时打印公告。

### 修复

- 修复微信公众号代码块使用一个 `<pre>` 包含多个并列 `<code>` 节点时只保留第一行的问题；现在会完整保留代码行、空行、`<br>` 换行、缩进、实体和嵌套文本，并生成完整 fenced Markdown 代码块。

### 测试

- 新增链接模式和粘贴 HTML 模式的多节点代码块 TDD 回归测试，并对用户提供的微信公众号文章执行内存中的逐代码块对照；对照保留代码标点、运算符和字符串符号。
- 通过 322 项自动化测试、Chromium/Firefox/WebKit 60/60 E2E、稳定真实网页 2/2 和全新 Apple Silicon 安装包校验。

### 维护

- 将已完成计划、任务记录、旧状态快照、WorkBuddy 数据和截至 0.2.0 的发布 ZIP 归档到仓库工作区外。
- 发布保护改为从外部归档校验不可变历史 ZIP 哈希，项目文件夹只保留最新构建输出。

## [0.2.0] - 2026-08-11

### Documentation

- 将 v0.2 合并到 `main` 并确认为当前正式版本；默认公开文档和 GitHub 元数据使用英文，同时保留 `.zh.md` 中文版本。
- 新增 v0.2 富文本粘贴转换规划（`docs/PLAN-V0.2.md`）与任务级追踪（`docs/TASKS-V0.2.md`）；Harness 新增 TDD 强制与 PLAN/TASK 分离约定（`AGENTS.md`）。
- 新增 Mermaid 图表保留规划与任务追踪（`docs/PLAN-FEAT-013.md`、`docs/TASKS-FEAT-013.md`），明确源码优先、链接动态 SVG 截图、粘贴 Mermaid SVG 安全栅格化与不引入预览渲染器的边界。
- 建立 `v0.1.3` 封版标签与独立 v0.2 开发分支，开发版本从开工起隔离为 `0.2.0`，并恢复 0.1.3 正式 ZIP 的只读外部归档。
- 完成 v0.2 开工评审：补齐 HTML/纯文本双通道、编辑后降级、语义门控、Data URI 完整校验、无来源 URL 资源规则、登录态图片限制和 T10-only live 门禁，并把过大的实施任务拆为可独立验证的 TDD 单元。
- 同步 feat-015 源码与 0.2.0 候选 ZIP：一键清空已完成源码、自动门禁、重新打包和真实窗口验收，当前候选包已包含该功能。
- 将微信公众号真实对照拆为可单独运行的非阻断诊断命令；正式发布仍以稳定的 WalkingLabs 链接/粘贴 Mermaid 真实样本作为联网阻断门禁。
- 新增 `feat-016` 独立规划与任务追踪，固化链接清空和长结果返回顶部的交互及测试边界。

### Added

- 新增“链接转换”和“富文本转换”双模式；富文本模式接收用户主动复制的 `text/html`/`text/plain`，支持语义 HTML、纯文本降级、编辑后降级、再次粘贴替换和可选来源 URL。
- 新增 `/api/convert-paste` 本地接口及 5 MiB UTF-8 有界请求体、HTML 净化、图片处理、统计、警告和取消/超时契约。
- 富文本模式新增“一键清空”，可同时移除当前 HTML、纯文本、来源 URL 和旧转换结果，便于直接处理下一份内容。
- 新增 Mermaid 保留：源码输出 fenced `mermaid` 代码块；链接页面只有客户端渲染结果时安全截图为 PNG；富文本中的 Mermaid SVG 经独立清洗后在本机转为 PNG；无法转换或只有 Canvas 时占位并提示。
- 链接模式新增“清空链接”，一次清除 URL、校验和旧结果；长结果滚动约 500px 后显示固定“返回顶部”，桌面与窄屏均可快速回到输入区。

### Security

- 富文本粘贴使用独立 DOMPurify 规则，不读取 Cookie 或登录态；远程图片沿用 SSRF 防护，`data:` 图片严格解码、格式校验和优化，登录态/临时签名/`blob:` 图片失败时降级为替代文本。
- Mermaid 不在浏览器中执行粘贴 SVG，也不允许网页任意 Data URI 进入链接模式；链接截图使用请求内可信映射，粘贴图移除脚本/外部资源后由 Sharp 栅格化，两者都重新经过栅格格式与图片预算校验。
- T9A 发布门禁固定保护 `v0.1.3` 历史标签、外部只读 0.1.3 归档和 0.1.0–0.1.3 四个历史 ZIP；非 `0.2.0` 目标在任何发布命令前拒绝，历史清单在成功和失败路径复核。
- 升级 Next.js 16.3.0、Undici 8.10.0、DOMPurify 3.4.13、Electron 43.3.0 及兼容 Nano ID 锁版本，生产依赖审计降为 0；完整开发/构建树剩余告警不进入打包应用。

### Fixed

- 粘贴 Mermaid 栅格图现在先铺设不透明白色背景，并在栅格化前移除来源主题 CSS、为节点、连线与文字写入固定的可读浅色配色；避免脱离网页后 SVG 默认回退为黑色而生成不可读黑图。
- Mermaid 质检整改会同时移除元素内联 `style`，防止其以更高优先级覆盖固定配色；应用生成的小型 PNG 使用内部标记通过图片校验，标记在 Markdown 输出前移除。
- 动态页面按倒序处理会被替换的 Mermaid 节点，避免多个图表因实时选择器索引变化而跳图；纯源码容器不再被截图流程误判为失败图。
- 富文本超过 30 张 Mermaid 时，额外图表会明确降级并计入来源/省略统计，同时返回 `MERMAID_COUNT_LIMIT` 警告。
- 动态页面现在阻止会挂起受控代理导航的 Google Fonts 样式表，但保留页面自身 CSS，避免 Mermaid 页面因字体服务超时而丢图。
- 适配 Electron 43 不再随 `npm install` 自动下载运行时的变化，桌面准备阶段现在显式调用官方 `install-electron`，避免新环境首次打包因缓存缺失失败。
- 远程图片现在同时校验声明 MIME 与 Sharp 检测到的实际格式，伪装成栅格图片的 SVG 或其他不匹配内容会降级为替代文本。
- 发布门禁现在直接解压并检查 ZIP 内应用的版本与 arm64 可执行文件，不再只依赖 Forge 输出目录旁路验证。

### Testing

- feat-016 按 TDD 固化链接清空、loading 禁用、390px/1180px 返回顶部与状态保留；Node.js 24.14.1 基线通过 317 tests，三引擎 E2E 60/60，真实 Electron 窗口验收通过。
- feat-016 完整发布门禁通过 317 tests、三引擎 60/60、WalkingLabs live 2/2、Forge 与 fresh ZIP 校验；新 0.2.0 arm64 ZIP 为 354,636,241 bytes，SHA-256 `5becae36...16485`。
- 新增 Mermaid 源码、粘贴 SVG 安全栅格化/失败降级、Chromium 截图/预算/取消、可信图片交接和 WalkingLabs 链接/粘贴真实网页门禁。
- feat-013 质检整改后的基线与真实窗口验收通过；微信上游超时改为非阻断诊断后，最终 `desktop:release` 通过 315 tests、三引擎 51/51、WalkingLabs 链接/粘贴 live 2/2、Forge 与 fresh ZIP 校验。新 0.2.0 arm64 ZIP 为 354,631,314 bytes，SHA-256 `b212b359...7d505c`。
- 新增富文本单元/API/三浏览器 E2E 验证；T10 连续通过 24 files / 282 tests、三引擎 48/48、真实微信门禁、Forge、fresh ZIP 与打包应用冒烟，用户已确认真实窗口人工验收通过；提交前安全整改后的最终基线为 24 files / 285 tests。
- feat-015 后重新运行 Node.js 24.14.1 完整发布门禁：287 tests、三引擎 51/51、真实微信门禁、Forge 与 fresh ZIP 全部通过；新包启动、`example.com` 转换和“一键清空”真实窗口验收通过。

## [0.1.3] - 2026-07-21

### Changed

- 结果操作按钮统一精简为“复制”和“下载”，复制成功后仍短暂显示“已复制”。
- Harness 固定使用 Node.js 24.x；E2E 改用 production standalone 服务并阻断测试引起的 tracked 文件变化。

### Fixed

- 新增应用图标资源，消除页面加载时缺少 `favicon.ico` 的浏览器控制台错误。
- 动态浏览器的所有 HTTP/HTTPS 请求改经逐请求校验并固定 IP 的回环代理，封堵 DNS 重绑定绕过 SSRF 防护的风险。
- Electron 每次启动生成随机会话令牌，本地转换 API 同时校验回环 Host、同源来源、JSON 类型和令牌。
- 正确将 Node.js `TimeoutError` 映射为 `504 CONVERSION_TIMEOUT`，并区分用户主动停止。
- 桌面准备过程显式复制 Sharp arm64 二进制及配套 libvips，避免新安装依赖后打包应用转换时报动态库缺失。
- 安全代理在 Chromium 取消 CONNECT 时不再向已关闭 socket 写入，消除预期取消场景的 `EPIPE`。
- 桌面冒烟失败现在返回非零进程码，发布脚本不会再把转换失败误判为成功。

### Added

- 新增 API、限流、动态浏览器、逐跳重定向、DNS 固定和完整转换编排测试，并把安全关键覆盖率阈值加入日常基线。
- 新增真实 HTTP 双向传输、并发子资源、流式响应、CONNECT、取消和 WebSocket 代理集成测试，并把代理覆盖率门禁提高到 85% lines / 75% branches / 90% functions。
- 发布流程新增 fresh ZIP、版本、arm64、包结构、大小和 SHA-256 自动校验。

### Security

- 动态浏览器代理新增每次回退 100 个请求、累计 50 MiB 和单 CONNECT 隧道 25 MiB 的共享预算；HTTP 请求体、响应或隧道超限时立即拒绝或关闭。

### Documentation

- 补充跨电脑使用条件，明确仅支持 Apple Silicon、最低 macOS 12.0、无需外部运行环境，以及未签名应用的首次启动方式。
- 新增测试与发布运行手册，统一说明三浏览器安装、真实网页门禁、打包环境变量、冒烟测试和 0.1.3 人工验收。
- 新增整体质检报告，记录发布判断、安全与依赖问题、整改顺序和复验清单。
- 更新安全架构、测试门禁和质检整改状态，记录 Node.js 24 复验要求。
- 清理质检报告中整改前的测试数量、旧产物哈希和旧阻断结论，并补充 Node.js 24 Forge 无产物退出及内部会话令牌的运行手册说明。
- 初始化本地 Git `main` 基线，并补充构建产物、环境变量、日志与 macOS 元数据的忽略规则。
- 完成第二轮整体质检：确认 Node.js 24 完整发布路径和第一轮安全整改，补充代理资源预算、发布 Harness 与测试工作区副作用问题。
- 完成第二台 Apple Silicon Mac 安装验收；记录未签名应用出现“文件已经损坏”提示时，在核对产物哈希后精确移除 quarantine 属性的处理方式。
- 取消尚未实施的 Windows、多平台仓库迁移和直接粘贴正文转换方案，删除失效的多平台规划文档并保持现有 0.1.3 Mac 产品边界。

## [0.1.2] - 2026-07-18

### Added

- 新增转换中停止功能，停止后保留原链接供修改重试。
- 新增文件大小、正文字数、源图片数、内嵌图片数和省略图片数统计。
- 新增合成网页—Markdown 精确金标准测试，以及不保存正文的微信公众号真实网页发布门禁。
- 新增 `npm run test:live` 与完整的 `npm run desktop:release` 发布命令。

### Changed

- 粘贴链接不再自动开始转换，需点击“转换为 MD”或按 Enter 确认。
- 结果区以“转换完成”为主状态，不再重复展示文章标题或向普通用户突出技术提取模式。
- 20 MiB 超限时优先保留全部正文，并从最后一张内嵌图片开始逐张省略。
- 桌面窗口下主副标题保持单行，更窄页面仍允许自然换行。

### Fixed

- 超过 8 MiB 的源图片现在显示独立的 `IMAGE_TOO_LARGE` 提示，不再归类为普通下载失败。
- 修复提交按钮切换为停止按钮时复用 DOM 节点，导致可信点击事件可能失效的问题。
- 相对正文链接现在会按原网页地址转换为绝对 HTTP/HTTPS 链接。

## [0.1.1] - 2026-07-18

### Added

- 初始化项目 Harness，包括项目级指令、事项状态、进度记录、验证入口和跨会话交接机制。
- 新增 Apple Silicon Mac 单链接桌面应用，支持粘贴即转换、预览、复制和保存 Markdown。
- 新增公开 URL 安全抓取、正文提取和 JavaScript 动态网页浏览器兜底。
- 新增图片优化与 Base64 单文件内嵌，最终文件限制为 20 MiB。
- 新增 Chromium、Firefox、WebKit 界面验收及转换管线单元与安全测试。
- 新增产品、架构文档和社交分享图。
- 新增 Electron 桌面外壳与 `darwin/arm64` 打包配置。
- 新增随应用分发的 Apple Silicon Chromium Headless Shell，动态网页转换不依赖系统浏览器。
- 新增可拷贝到其他 Apple Silicon Mac 的 ZIP 测试产物。

### Changed

- 第一阶段固定为无需服务器、域名、账号或 AI API 的 Apple Silicon Mac 单机应用。
- 明确应用代码不建立转换历史；Electron/Chromium 仍会创建本机运行缓存。

### Fixed

- 修复微信公众号隐藏正文容器被 Readability 忽略、结果只剩封面摘要和封面图的问题。
- 修复微信公众号正文图片的 `data-src` 在净化时被删除，导致多图文章无法内嵌图片的问题。
- 修复微信公众号“环境异常”验证页被误判为转换成功的问题。
- 请求改用桌面浏览器 User-Agent，降低公开微信文章被错误拦截的概率。
- 微信文章需要人工验证、已删除或违规不可见时返回明确错误。
- 修复部分图片域名触发 Node 多地址 DNS 查询时下载失败，导致预览和 Markdown 文件缺少内嵌图片的问题。
