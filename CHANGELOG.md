# Changelog

本文件记录项目中面向用户或协作者的重要变化，格式参考 Keep a Changelog；尚未归入版本的变化记录在 `[Unreleased]`。

## [Unreleased]

### Documentation

- 新增 v0.2 富文本粘贴转换规划（`docs/PLAN-V0.2.md`）与任务级追踪（`docs/TASKS-V0.2.md`）；Harness 新增 TDD 强制与 PLAN/TASK 分离约定（`AGENTS.md`）。
- 新增 Mermaid 图表保留规划与任务追踪（`docs/PLAN-FEAT-013.md`、`docs/TASKS-FEAT-013.md`），明确源码优先、链接动态 SVG 截图、粘贴 Mermaid SVG 安全栅格化与不引入预览渲染器的边界。
- 建立 `v0.1.3` 封版标签与独立 v0.2 开发分支，开发版本从开工起隔离为 `0.2.0`，并恢复 0.1.3 正式 ZIP 的只读外部归档。
- 完成 v0.2 开工评审：补齐 HTML/纯文本双通道、编辑后降级、语义门控、Data URI 完整校验、无来源 URL 资源规则、登录态图片限制和 T10-only live 门禁，并把过大的实施任务拆为可独立验证的 TDD 单元。
- 同步 feat-015 源码与 0.2.0 候选 ZIP：一键清空已完成源码、自动门禁、重新打包和真实窗口验收，当前候选包已包含该功能。
- 将微信公众号真实对照拆为可单独运行的非阻断诊断命令；正式发布仍以稳定的 WalkingLabs 链接/粘贴 Mermaid 真实样本作为联网阻断门禁。
- 新增 `feat-016` 独立规划与任务追踪，固化链接清空和长结果返回顶部的交互及测试边界。

### Added

- 新增“链接转换”和“富文本转换”双模式；富文本模式接收用户主动复制的 `text/html`/`text/plain`，支持语义 HTML、纯文本降级、编辑后降级、再次粘贴替换和可选来源 URL。
- 新增 `/api/convert-paste` 本地接口及 5 MiB UTF-8 有界请求体、HTML 净化、图片处理、统计、警告和取消/超时契约。
- 富文本模式新增“一键清空”，可同时移除当前 HTML、纯文本、来源 URL 和旧转换结果，便于直接处理下一份内容。
- 新增 Mermaid 保留：源码输出 fenced `mermaid` 代码块；链接页面只有客户端渲染结果时安全截图为 PNG；富文本中的 Mermaid SVG 经独立清洗后在本机转为 PNG；无法转换或只有 Canvas 时占位并提示。
- 链接模式新增“清空链接”，一次清除 URL、校验和旧结果；长结果滚动约 500px 后显示固定“返回顶部”，桌面与窄屏均可快速回到输入区。

### Security

- 富文本粘贴使用独立 DOMPurify 规则，不读取 Cookie 或登录态；远程图片沿用 SSRF 防护，`data:` 图片严格解码、格式校验和优化，登录态/临时签名/`blob:` 图片失败时降级为替代文本。
- Mermaid 不在浏览器中执行粘贴 SVG，也不允许网页任意 Data URI 进入链接模式；链接截图使用请求内可信映射，粘贴图移除脚本/外部资源后由 Sharp 栅格化，两者都重新经过栅格格式与图片预算校验。
- T9A 发布门禁固定保护 `main`/`v0.1.3`、外部只读 0.1.3 归档和 0.1.0–0.1.3 四个历史 ZIP；非 `0.2.0` 目标在任何发布命令前拒绝，历史清单在成功和失败路径复核。
- 升级 Next.js 16.3.0、Undici 8.10.0、DOMPurify 3.4.13、Electron 43.3.0 及兼容 Nano ID 锁版本，生产依赖审计降为 0；完整开发/构建树剩余告警不进入打包应用。

### Fixed

- 粘贴 Mermaid 栅格图现在先铺设不透明白色背景，并在栅格化前移除来源主题 CSS、为节点、连线与文字写入固定的可读浅色配色；避免脱离网页后 SVG 默认回退为黑色而生成不可读黑图。
- Mermaid 质检整改会同时移除元素内联 `style`，防止其以更高优先级覆盖固定配色；应用生成的小型 PNG 使用内部标记通过图片校验，标记在 Markdown 输出前移除。
- 动态页面按倒序处理会被替换的 Mermaid 节点，避免多个图表因实时选择器索引变化而跳图；纯源码容器不再被截图流程误判为失败图。
- 富文本超过 30 张 Mermaid 时，额外图表会明确降级并计入来源/省略统计，同时返回 `MERMAID_COUNT_LIMIT` 警告。
- 动态页面现在阻止会挂起受控代理导航的 Google Fonts 样式表，但保留页面自身 CSS，避免 Mermaid 页面因字体服务超时而丢图。
- 适配 Electron 43 不再随 `npm install` 自动下载运行时的变化，桌面准备阶段现在显式调用官方 `install-electron`，避免新环境首次打包因缓存缺失失败。
- 远程图片现在同时校验声明 MIME 与 Sharp 检测到的实际格式，伪装成栅格图片的 SVG 或其他不匹配内容会降级为替代文本。
- 发布门禁现在直接解压并检查 ZIP 内应用的版本与 arm64 可执行文件，不再只依赖 Forge 输出目录旁路验证。

### Testing

- feat-016 按 TDD 固化链接清空、loading 禁用、390px/1180px 返回顶部与状态保留；Node.js 24.14.1 基线通过 317 tests，三引擎 E2E 60/60，真实 Electron 窗口验收通过。
- feat-016 完整发布门禁通过 317 tests、三引擎 60/60、WalkingLabs live 2/2、Forge 与 fresh ZIP 校验；新 0.2.0 arm64 ZIP 为 354,636,241 bytes，SHA-256 `5becae36...16485`。
- 新增 Mermaid 源码、粘贴 SVG 安全栅格化/失败降级、Chromium 截图/预算/取消、可信图片交接和 WalkingLabs 链接/粘贴真实网页门禁。
- feat-013 质检整改后的基线与真实窗口验收通过；微信上游超时改为非阻断诊断后，最终 `desktop:release` 通过 315 tests、三引擎 51/51、WalkingLabs 链接/粘贴 live 2/2、Forge 与 fresh ZIP 校验。新 0.2.0 arm64 ZIP 为 354,631,314 bytes，SHA-256 `b212b359...7d505c`。
- 新增富文本单元/API/三浏览器 E2E 验证；T10 连续通过 24 files / 282 tests、三引擎 48/48、真实微信门禁、Forge、fresh ZIP 与打包应用冒烟，用户已确认真实窗口人工验收通过；提交前安全整改后的最终基线为 24 files / 285 tests。
- feat-015 后重新运行 Node.js 24.14.1 完整发布门禁：287 tests、三引擎 51/51、真实微信门禁、Forge 与 fresh ZIP 全部通过；新包启动、`example.com` 转换和“一键清空”真实窗口验收通过。

## [0.1.3] - 2026-07-21

### Changed

- 结果操作按钮统一精简为“复制”和“下载”，复制成功后仍短暂显示“已复制”。
- Harness 固定使用 Node.js 24.x；E2E 改用 production standalone 服务并阻断测试引起的 tracked 文件变化。

### Fixed

- 新增应用图标资源，消除页面加载时缺少 `favicon.ico` 的浏览器控制台错误。
- 动态浏览器的所有 HTTP/HTTPS 请求改经逐请求校验并固定 IP 的回环代理，封堵 DNS 重绑定绕过 SSRF 防护的风险。
- Electron 每次启动生成随机会话令牌，本地转换 API 同时校验回环 Host、同源来源、JSON 类型和令牌。
- 正确将 Node.js `TimeoutError` 映射为 `504 CONVERSION_TIMEOUT`，并区分用户主动停止。
- 桌面准备过程显式复制 Sharp arm64 二进制及配套 libvips，避免新安装依赖后打包应用转换时报动态库缺失。
- 安全代理在 Chromium 取消 CONNECT 时不再向已关闭 socket 写入，消除预期取消场景的 `EPIPE`。
- 桌面冒烟失败现在返回非零进程码，发布脚本不会再把转换失败误判为成功。

### Added

- 新增 API、限流、动态浏览器、逐跳重定向、DNS 固定和完整转换编排测试，并把安全关键覆盖率阈值加入日常基线。
- 新增真实 HTTP 双向传输、并发子资源、流式响应、CONNECT、取消和 WebSocket 代理集成测试，并把代理覆盖率门禁提高到 85% lines / 75% branches / 90% functions。
- 发布流程新增 fresh ZIP、版本、arm64、包结构、大小和 SHA-256 自动校验。

### Security

- 动态浏览器代理新增每次回退 100 个请求、累计 50 MiB 和单 CONNECT 隧道 25 MiB 的共享预算；HTTP 请求体、响应或隧道超限时立即拒绝或关闭。

### Documentation

- 补充跨电脑使用条件，明确仅支持 Apple Silicon、最低 macOS 12.0、无需外部运行环境，以及未签名应用的首次启动方式。
- 新增测试与发布运行手册，统一说明三浏览器安装、真实网页门禁、打包环境变量、冒烟测试和 0.1.3 人工验收。
- 新增整体质检报告，记录发布判断、安全与依赖问题、整改顺序和复验清单。
- 更新安全架构、测试门禁和质检整改状态，记录 Node.js 24 复验要求。
- 清理质检报告中整改前的测试数量、旧产物哈希和旧阻断结论，并补充 Node.js 24 Forge 无产物退出及内部会话令牌的运行手册说明。
- 初始化本地 Git `main` 基线，并补充构建产物、环境变量、日志与 macOS 元数据的忽略规则。
- 完成第二轮整体质检：确认 Node.js 24 完整发布路径和第一轮安全整改，补充代理资源预算、发布 Harness 与测试工作区副作用问题。
- 完成第二台 Apple Silicon Mac 安装验收；记录未签名应用出现“文件已经损坏”提示时，在核对产物哈希后精确移除 quarantine 属性的处理方式。
- 取消尚未实施的 Windows、多平台仓库迁移和直接粘贴正文转换方案，删除失效的多平台规划文档并保持现有 0.1.3 Mac 产品边界。

## [0.1.2] - 2026-07-18

### Added

- 新增转换中停止功能，停止后保留原链接供修改重试。
- 新增文件大小、正文字数、源图片数、内嵌图片数和省略图片数统计。
- 新增合成网页—Markdown 精确金标准测试，以及不保存正文的微信公众号真实网页发布门禁。
- 新增 `npm run test:live` 与完整的 `npm run desktop:release` 发布命令。

### Changed

- 粘贴链接不再自动开始转换，需点击“转换为 MD”或按 Enter 确认。
- 结果区以“转换完成”为主状态，不再重复展示文章标题或向普通用户突出技术提取模式。
- 20 MiB 超限时优先保留全部正文，并从最后一张内嵌图片开始逐张省略。
- 桌面窗口下主副标题保持单行，更窄页面仍允许自然换行。

### Fixed

- 超过 8 MiB 的源图片现在显示独立的 `IMAGE_TOO_LARGE` 提示，不再归类为普通下载失败。
- 修复提交按钮切换为停止按钮时复用 DOM 节点，导致可信点击事件可能失效的问题。
- 相对正文链接现在会按原网页地址转换为绝对 HTTP/HTTPS 链接。

## [0.1.1] - 2026-07-18

### Added

- 初始化项目 Harness，包括项目级指令、事项状态、进度记录、验证入口和跨会话交接机制。
- 新增 Apple Silicon Mac 单链接桌面应用，支持粘贴即转换、预览、复制和保存 Markdown。
- 新增公开 URL 安全抓取、正文提取和 JavaScript 动态网页浏览器兜底。
- 新增图片优化与 Base64 单文件内嵌，最终文件限制为 20 MiB。
- 新增 Chromium、Firefox、WebKit 界面验收及转换管线单元与安全测试。
- 新增产品、架构文档和社交分享图。
- 新增 Electron 桌面外壳与 `darwin/arm64` 打包配置。
- 新增随应用分发的 Apple Silicon Chromium Headless Shell，动态网页转换不依赖系统浏览器。
- 新增可拷贝到其他 Apple Silicon Mac 的 ZIP 测试产物。

### Changed

- 第一阶段固定为无需服务器、域名、账号或 AI API 的 Apple Silicon Mac 单机应用。
- 明确应用代码不建立转换历史；Electron/Chromium 仍会创建本机运行缓存。

### Fixed

- 修复微信公众号隐藏正文容器被 Readability 忽略、结果只剩封面摘要和封面图的问题。
- 修复微信公众号正文图片的 `data-src` 在净化时被删除，导致多图文章无法内嵌图片的问题。
- 修复微信公众号“环境异常”验证页被误判为转换成功的问题。
- 请求改用桌面浏览器 User-Agent，降低公开微信文章被错误拦截的概率。
- 微信文章需要人工验证、已删除或违规不可见时返回明确错误。
- 修复部分图片域名触发 Node 多地址 DNS 查询时下载失败，导致预览和 Markdown 文件缺少内嵌图片的问题。
